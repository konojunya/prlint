name: PR Lint
on:
  pull_request:
    types: [opened, synchronize, reopened, edited]

permissions:
  contents: read

jobs:
  prlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: konojunya/prlint@v1.0.3
